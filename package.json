{
  "name": "postman-newman-github",
  "version": "1.0.0",
  "description": "Storing potman test data",
  
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-test-production": "newman run tests/JSONplaceholder.com.postman_collection.json --reporters cli,junit --reporter-junit-export newman.xml"
  },
  "author": "Harshal Bhore",
  
  "dependencies": {
    "newman": "^4.5.0"
  }
}
